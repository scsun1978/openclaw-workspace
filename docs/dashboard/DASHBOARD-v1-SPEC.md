# Dashboard v1 设计规格

- 版本：v1.0
- 日期：2026-02-18
- 目标：为 OpenClaw 多 Agent 系统提供可视化监控与治理能力

---

## 1) 数据模型

### 1.1 核心实体

#### Session（会话）
```typescript
interface Session {
  id: string;                    // 会话 ID
  agentId: string;               // Agent 标识（monitor/code/qa/docs）
  channel: string;               // 渠道（telegram/webchat）
  status: 'active' | 'idle' | 'error' | 'closed';
  createdAt: timestamp;
  lastActivityAt: timestamp;
  contextTokens: number;         // 上下文 Token 数
  model: string;                 // 使用的模型
  messageCount: number;          // 消息数
  errorCount: number;            // 错误数
  cost: number;                  // 成本（USD）
}
```

#### Task（任务）
```typescript
interface Task {
  id: string;                    // 任务 ID
  project: string;               // 所属项目
  stage: string;                 // 当前阶段
  owner: string;                 // 负责人（agentId）
  status: 'pending' | 'in-progress' | 'done' | 'failed' | 'blocked';
  createdAt: timestamp;
  startedAt: timestamp | null;
  completedAt: timestamp | null;
  deadline: timestamp | null;
  stalledMinutes: number;        // 停滞时长
  pushAttempts: number;          // 推送次数
  output: string;                // 输出摘要
}
```

#### Alert（告警）
```typescript
interface Alert {
  id: string;
  type: 'stall' | 'error' | 'cost' | 'debt' | 'gate';
  severity: 'P0' | 'P1' | 'P2';
  source: string;                // 来源（agentId/projectId）
  message: string;
  createdAt: timestamp;
  acknowledgedAt: timestamp | null;
  resolvedAt: timestamp | null;
  escalateTo: string | null;     // 升级目标
}
```

#### Metric（指标）
```typescript
interface Metric {
  timestamp: timestamp;
  type: 'token' | 'cost' | 'latency' | 'error_rate' | 'coverage';
  agentId: string;
  model: string;
  value: number;
  tags: Record<string, string>;
}
```

#### Debt（技术债务）
```typescript
interface Debt {
  id: string;
  description: string;
  source: string;                // 来源阶段（W1/W2/W3）
  status: 'open' | 'in-progress' | 'closed';
  priority: 'P0' | 'P1' | 'P2';
  owner: string;
  createdAt: timestamp;
  dueDate: timestamp | null;
  closedAt: timestamp | null;
}
```

### 1.2 数据采集点

| 数据源 | 采集方式 | 频率 | 存储位置 |
|--------|----------|------|----------|
| Session 日志 | 解析 `.jsonl` 文件 | 实时 | 时序数据库 |
| Team-Tasks | 读取 `.json` 文件 | 30s | 内存 + 持久化 |
| 执行日志 | 解析 `logs/` 目录 | 实时 | 时序数据库 |
| Model 统计 | Skill 输出 + 运行时 | 1min | 时序数据库 |
| Memory 索引 | 元数据查询 | 5min | 内存 |

---

## 2) 页面布局

### 2.1 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  Header: OpenClaw Dashboard | 时间 | 系统状态               │
├─────────────────────────────────────────────────────────────┤
│  Nav: 概览 | Agent | 任务 | 告警 | 债务 | 分析 | 配置       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                      Main Content Area                      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  Footer: 版本 | 最后刷新 | 快捷操作                          │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 页面清单

#### P1: 概览页（Overview）
```
┌───────────────────┬───────────────────┬───────────────────┐
│  系统健康度        │  活跃会话数        │  今日成本          │
│  🟢 GREEN         │  12               │  $2.34            │
└───────────────────┴───────────────────┴───────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Agent 活跃度热力图                                          │
│  [monitor ████████░░ 80%] [code ██████░░░░ 60%]             │
│  [qa ████░░░░░░ 40%] [docs ████████░░ 80%]                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  任务流水线状态                                              │
│  stock-simgame-live: W3 [███░░░░░░░] NO-GO 🔴              │
│  stock-simgame-impl: done [██████████] PASS ✅              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  最近告警（TOP 5）                                           │
│  - P0 Docker 环境缺口（stock-simgame-live）                 │
│  - P1 覆盖率报告缺失                                         │
└─────────────────────────────────────────────────────────────┘
```

#### P2: Agent 监控页
```
┌─────────────────────────────────────────────────────────────┐
│  Agent 选择器：[monitor ▼] [code] [qa] [docs]               │
└─────────────────────────────────────────────────────────────┘

┌───────────────┬───────────────┬───────────────┬─────────────┐
│  活跃会话      │  上下文占用    │  今日成本      │  错误率     │
│  5            │  45%          │  $0.87        │  2.3%       │
└───────────────┴───────────────┴───────────────┴─────────────┘

┌─────────────────────────────────────────────────────────────┐
│  模型调用分布（24h）                                         │
│  gpt-5.3-codex ████████████████████ 85%                     │
│  glm-5        ████ 15%                                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  最近会话列表                                                │
│  | 时间 | 状态 | 消息数 | 成本 | 操作 |                      │
│  | 14:30 | active | 12 | $0.12 | [详情] |                   │
└─────────────────────────────────────────────────────────────┘
```

#### P3: 任务看板页
```
┌─────────────────────────────────────────────────────────────┐
│  项目筛选：[全部 ▼] | 阶段筛选：[全部 ▼]                     │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┬──────────────────┬──────────────────┐
│  待处理 (Pending) │  进行中 (Active)  │  已完成 (Done)   │
├──────────────────┼──────────────────┼──────────────────┤
│  Task-001        │  Task-003        │  Task-002        │
│  Owner: code     │  Owner: qa       │  Owner: docs     │
│  停滞: 15min     │  进度: 60%       │  ✅ 完成         │
├──────────────────┼──────────────────┼──────────────────┤
│  Task-004        │                  │  Task-005        │
│  Owner: monitor  │                  │  Owner: code     │
│  停滞: 45min 🔴  │                  │  ✅ 完成         │
└──────────────────┴──────────────────┴──────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  停滞任务详情                                                │
│  Task-004: stock-simgame-live W3 Gate                        │
│  停滞原因: 等待 QA 实跑证据                                  │
│  推送次数: 3/3 (已达上限)                                   │
│  建议动作: 人工催办                                          │
└─────────────────────────────────────────────────────────────┘
```

#### P4: 告警中心页
```
┌─────────────────────────────────────────────────────────────┐
│  告警统计：P0(2) | P1(3) | P2(5) | 已确认(4) | 已解决(6)    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  活跃告警列表                                                │
│  | ID | 严重度 | 类型 | 来源 | 时间 | 状态 | 操作 |         │
│  | A001 | P0 | stall | qa-agent | 14:20 | 未确认 | [确认] |│
│  | A002 | P0 | debt | stock-simgame | 13:45 | 已确认 | [解决] |│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  告警历史趋势（7d）                                          │
│  [折线图：按日期显示 P0/P1/P2 数量]                          │
└─────────────────────────────────────────────────────────────┘
```

#### P5: 技术债务页
```
┌─────────────────────────────────────────────────────────────┐
│  债务统计：P0(3) | P1(2) | 总计(5) | 本周新增(1)            │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  债务看板                                                    │
│  | ID | 描述 | 来源 | 优先级 | Owner | 状态 | ETA |         │
│  | D1 | Docker 环境缺口 | W1 | P0 | qa | open | - |        │
│  | D2 | 覆盖率报告缺失 | W1 | P0 | qa | open | - |         │
│  | D3 | Runbook 演练未执行 | W2 | P1 | docs | open | - |   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  债务趋势（按阶段）                                          │
│  W1: ██ (2) | W2: █ (1) | W3: ██ (2)                        │
└─────────────────────────────────────────────────────────────┘
```

#### P6: 分析报表页
```
┌─────────────────────────────────────────────────────────────┐
│  时间范围：[今日 ▼] [自定义]                                 │
└─────────────────────────────────────────────────────────────┘

┌───────────────┬───────────────┬───────────────┬─────────────┐
│  总会话数      │  总成本        │  平均响应时间  │  错误率     │
│  307          │  $15.23       │  2.3s         │  3.2%       │
└───────────────┴───────────────┴───────────────┴─────────────┘

┌─────────────────────────────────────────────────────────────┐
│  模型使用分布                                                │
│  [饼图：gpt-5.3-codex 92.8% / glm-5 7.2%]                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Agent 效率对比                                              │
│  [柱状图：完成任务数 / 平均响应时间 / 错误率]                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  成本趋势（7d）                                              │
│  [折线图：每日成本累计]                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 3) 告警规则

### 3.1 告警级别定义

| 级别 | 触发条件 | 响应时间 | 升级路径 |
|------|----------|----------|----------|
| **P0** | 阻断发布/核心功能失效 | 5min | Agent Owner → Monitor → 项目 Owner |
| **P1** | 影响里程碑但可绕行 | 30min | Agent Owner → Monitor |
| **P2** | 优化项/体验问题 | 4h | Agent Owner |

### 3.2 告警规则清单

#### 任务停滞告警
```yaml
rule: task_stall
trigger:
  - task.status == 'in-progress' AND task.stalledMinutes > threshold
  - threshold: P0=30min, P1=60min, P2=120min
action:
  - create_alert(severity=P0/P1/P2)
  - if push_attempts < 3: auto_push()
  - else: escalate_to_monitor()
```

#### 错误率告警
```yaml
rule: error_rate_spike
trigger:
  - session.error_rate > 10% in 5min window
action:
  - create_alert(severity=P1)
  - notify_agent_owner()
```

#### 成本告警
```yaml
rule: cost_threshold
trigger:
  - daily_cost > $20
  - hourly_cost > $5
action:
  - create_alert(severity=P2)
  - notify_monitor()
```

#### 技术债务告警
```yaml
rule: debt_overdue
trigger:
  - debt.dueDate < now() AND debt.status != 'closed'
action:
  - create_alert(severity=debt.priority)
  - escalate_to_owner()
```

#### Gate 结论告警
```yaml
rule: gate_no_go
trigger:
  - gate.conclusion == 'NO-GO'
action:
  - create_alert(severity=P0)
  - notify_all_owners()
  - freeze_new_features()
```

#### 模型 Fallback 告警
```yaml
rule: model_fallback
trigger:
  - fallback_count > 3 in 1h window
action:
  - create_alert(severity=P1)
  - notify_monitor()
```

### 3.3 告警升级矩阵

| 场景 | 初次告警 | 15min 未处理 | 30min 未处理 | 1h 未处理 |
|------|----------|--------------|--------------|-----------|
| P0 任务停滞 | Agent Owner | Monitor | 项目 Owner | 冻结开发 |
| P0 Gate NO-GO | 全员 | 项目 Owner | 冻结发布 | - |
| P1 错误率 | Agent Owner | Monitor | 项目 Owner | - |
| P2 成本 | Monitor | - | - | - |

---

## 4) 技术实现建议

### 4.1 技术栈

| 组件 | 推荐方案 | 说明 |
|------|----------|------|
| 后端 | Node.js + Express | 轻量、易集成 |
| 数据库 | SQLite / PostgreSQL | 本地开发 / 生产环境 |
| 时序数据 | InfluxDB / TimescaleDB | 指标存储 |
| 前端 | React + TypeScript | 组件化、类型安全 |
| 图表 | Recharts / ECharts | 丰富图表类型 |
| 实时更新 | WebSocket | 实时推送告警 |

### 4.2 数据采集层

```typescript
// 采集器接口
interface Collector {
  name: string;
  interval: number;  // 采集间隔（ms）
  collect(): Promise<DataPoint[]>;
}

// 示例：Session 日志采集器
class SessionLogCollector implements Collector {
  name = 'session-log';
  interval = 5000;
  
  async collect() {
    const files = await glob('~/.openclaw/workspace/*.jsonl');
    return parseSessionLogs(files);
  }
}
```

### 4.3 告警引擎

```typescript
class AlertEngine {
  rules: AlertRule[];
  
  async evaluate(metrics: Metric[]) {
    for (const rule of this.rules) {
      const triggered = rule.evaluate(metrics);
      if (triggered) {
        await this.fireAlert(rule, metrics);
      }
    }
  }
  
  async fireAlert(rule: AlertRule, context: any) {
    const alert = createAlert(rule, context);
    await storeAlert(alert);
    await notify(alert);
    
    if (rule.autoAction) {
      await rule.autoAction.execute();
    }
  }
}
```

---

## 5) 实施路线

### Phase 1（1 周）：MVP
- 数据采集层（Session、Task、Alert）
- 概览页基础版
- 告警规则（停滞、错误率）

### Phase 2（2 周）：完整功能
- 全部页面实现
- 完整告警规则
- 历史数据查询

### Phase 3（1 周）：优化与集成
- 实时更新（WebSocket）
- 性能优化
- 与 OpenClaw Gateway 集成

---

**本规格可直接用于开发实施。**
