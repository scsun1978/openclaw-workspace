---
name: mission-control
description: 自动化 Mission Control 看板同步。定期从 OpenClaw 工作流提取数据，更新 Obsidian Canvas 实现可视化。支持 Tasks Board、Team、Calendar、Memory 四大组件。
metadata:
  openclaw:
    emoji: "🎛️"
---

# Mission Control 自动化

## 架构

```
┌─────────────────────────────────────────────────────────┐
│                   OpenClaw Runtime                       │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │ Monitor │  │  Code   │  │  Docs   │  │   QA    │    │
│  │ Agent   │  │ Agent   │  │ Agent   │  │ Agent   │    │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘    │
│       │            │            │            │          │
│       └────────────┴────────────┴────────────┘          │
│                          │                              │
│                   ┌──────▼──────┐                       │
│                   │ team-tasks/ │  ← 任务 JSON          │
│                   │  *.json     │                       │
│                   └──────┬──────┘                       │
└──────────────────────────┼──────────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │  mission-control-sync   │  ← 同步脚本
              │  (cron / heartbeat)     │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │    Obsidian Vault       │
              │  Mission Control/       │
              │  ├── Tasks Board.canvas │
              │  ├── Team.canvas        │
              │  ├── Calendar.canvas    │
              │  └── Memory.canvas      │
              └─────────────────────────┘
```

## 数据源

### 1. team-tasks 项目文件
- 位置: `~/.openclaw/workspace/team-tasks/projects/*.json`
- 包含: code/docs/qa/monitor 各阶段任务状态

### 2. cron jobs
- 来源: `cron list` 命令
- 包含: 所有定时任务

### 3. agent 会话
- 来源: `sessions_list` 命令
- 包含: 运行中的 sub-agents

### 4. memory 文件
- 来源: `~/.openclaw/workspace/memory/*.md`
- 包含: 所有记忆记录

## 同步频率

| 组件 | 频率 | 触发方式 |
|------|------|----------|
| Tasks Board | 5分钟 | cron |
| Team | 5分钟 | cron |
| Calendar | 15分钟 | cron |
| Memory | 30分钟 | heartbeat |

## Canvas 更新逻辑

### Tasks Board
```
读取 team-tasks/*.json
├── 提取所有任务
├── 按 status 分组 (todo/in-progress/review/done)
├── 生成 Canvas nodes
│   ├── 每个任务 = file node (链接到 Tasks/TASK-xxx.md)
│   └── 按 x 坐标排列 (todo:-400, progress:-100, review:200, done:500)
└── 写入 Tasks Board.canvas
```

### Team
```
读取 sessions_list
├── 检查各 agent 运行状态
├── 更新 Team.canvas 中的状态节点
│   ├── 运行中 → 绿色
│   ├── 空闲 → 灰色
│   └── 错误 → 红色
└── 添加当前任务信息
```

### Calendar
```
读取 cron list
├── 提取所有 scheduled tasks
├── 生成时间线节点
└── 标注下次执行时间
```

## 使用

### 手动同步
```bash
python3 ~/.openclaw/workspace/skills/mission-control/scripts/sync.py
```

### 启用自动同步 (cron)
```bash
# 添加到 OpenClaw cron
# 每 5 分钟同步 Tasks + Team
# 每 15 分钟同步 Calendar
```

### 在对话中触发
告诉 OpenClaw:
- "同步 Mission Control"
- "更新看板"
- "显示任务状态"
